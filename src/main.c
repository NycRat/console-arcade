#include "stuff.h"
#include "sortingVisualizer.h"
#include "pong.h"

int main()
{
  // for (int i=0; i<=255; i++)
  // {
  //   // 160 a
  //   printf("%c", 178);
  // }
  // getch();
  runSorting();
  setConsoleColour(FOREGROUND_RED);
  // runPong();
  setCursorPosition(0,0);
  printf("xd");
  getch();
}